# Automated dependency version updates for Go modules and GitHub Actions (opens PRs).
# Enable dependency security alerts in the repository Security settings separately.
version: 2
updates:
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
